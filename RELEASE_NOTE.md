## Values cache
Cache ``arg`` and ``placeholder`` calculate result, return cached value when repeat getting value.

## Bug fixes
Fix bug of multiple using ``arg`` or ``placeholder`` value with delegate mode causes error.